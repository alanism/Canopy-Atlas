# Public QA Audit Report

Status: `public_sample_mode_ready`

The public repository defaults to sample fixture mode and does not require private datasets. External warehouse examples must use placeholders such as `YOUR_PROJECT_ID.YOUR_DATASET`.

Required copy posture:

- `Observed Stablecoin Route Benchmark`
- `"suitable_for_runtime_routing": false`
- `sample_mode_ready_external_data_optional`

The 20-transaction live audit is intentionally out of scope for the public fixture build.
